(program
  (main () unit
    (block
      (let v1 nat (scope hint
        (block
          (let v0 nat (lit 15))
          (ret v0))))
      (let v4 nat (scope hint
        (block
          (let v2 nat (lit 2))
          (let v3 nat (mul v1 v2))
          (ret v3))))
      (let v5 nat (lit 30))
      (let v6 bool (eq v4 v5))
      (let v7 unit (op assert v6))
      (ret v7))))
